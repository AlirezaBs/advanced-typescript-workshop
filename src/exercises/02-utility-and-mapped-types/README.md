# Module 02 — Utility and Mapped Types

Derive form, preview, patch, and permission types from a `User` model using `Pick`, `Omit`, `Partial`, `Record`, and custom utilities.

See `Warmup.ts` TODOs and `Exercise.tsx` for the admin form challenge.
