# Module 03 — Discriminated Unions

Model `RequestState<T>`, `AsyncBoundary`, and payment lifecycle states where each variant exposes only valid fields.

See `Warmup.ts`, `Exercise.tsx`, and `Challenge.tsx` for TODOs.
